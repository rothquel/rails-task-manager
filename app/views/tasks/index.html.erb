<h1>All tasks</h1>
<ul>
<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task) %> ( <%= task.details %> ) done = <%= task.completed %> </li>
<% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path %>
